import { View, Text } from "react-native";
import React from "react";

const ProfileScreen = () => {
  return (
    <View className="mt-10">
      <Text className="text-red-600">ProfileScreen</Text>
    </View>
  );
};

export default ProfileScreen;
